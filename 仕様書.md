# 情報ネットワーク特論 最終課題：仮想ネットワーク上のP2Pブロックチェーンシステム仕様書

## 1. システムの目的と概要
本システムは、分散システムにおける通信とデータ同期の挙動を実証実験することを目的としています。
Linuxのネットワークネームスペース（netns）を用いて仮想的なマルチノード環境を構築し、ゼロからPythonで自作したブロックチェーンプログラムをP2P（Peer-to-Peer）ネットワーク上で稼働させます。

---

## 2. インフラ層（仮想ネットワーク構成）
物理的な複数台のPCを用意する代わりに、Linuxカーネルの機能である `netns` を利用して論理的に分離されたネットワーク環境を構築しています。

- **技術要素**: Linux Network Namespaces (`ip netns`), 仮想Ethernet (`veth`), 仮想ブリッジ (`br0`)
- **トポロジー**: スター型トポロジー。3つの独立したノードを仮想ブリッジに接続しています。
- **ノード構成とIPアドレス**:
  - `node1` (IP: `10.0.0.1/24`)
  - `node2` (IP: `10.0.0.2/24`)
  - `node3` (IP: `10.0.0.3/24`)
- **通信制御**: ブリッジを跨ぐ通信をホスト側の `iptables` の `FORWARD` チェーンで明示的に許可（ACCEPT）し、ノード間の疎通を確保しています。

---

## 3. ブロックチェーン層（コアロジック）
ビットコインなどのパブリックブロックチェーンの根幹となる技術をPythonの標準ライブラリ（`hashlib`, `json` 等）のみを用いて再現しています。

### 3.1 ブロックのデータ構造 (`Block` クラス)
各ブロックは以下のデータを保持します。
- `transaction`: トランザクション（取引）のデータ内容
- `prev_hash`: 1つ前のブロックのハッシュ値。これによりチェーン状の連続性が担保されます。
- `created_at`: ブロック作成時のタイムスタンプ
- `nonce`: マイニング（Proof of Work）によって求められる使い捨ての数値
- `hash`: ブロック固有の識別子。`SHA-256(transaction + prev_hash + created_at)` によって算出されます。

### 3.2 マイニングとProof of Work (PoW)
悪意のあるノードによる改ざんを防ぐため、新しいブロックをネットワークに追加する権利を得るための計算競争（PoW）を実装しています。
- **条件**: `SHA-256(ブロック固有のハッシュ + nonce)` の計算結果の先頭が、指定された難易度（Difficulty = 4）の数だけ `0` が連続すること（例: `0000...`）。
- プログラムは、この条件を満たすハッシュ値が見つかるまで `nonce` の値を1ずつ増やしながら総当たりで計算（探索）を繰り返します。

### 3.3 ジェネシスブロック（Genesis Block）
ブロックチェーンの最初のブロックです。分散ネットワーク上の全ノードが「全く同じ世界線」からチェーンを開始できるよう、ジェネシスブロックのみタイムスタンプを固定値にし、全ノードで同一のハッシュ値を持つように設計されています。

---

## 4. P2P通信層（ノード間連携）
各ノードは中央サーバーを介さず、お互いに直接対等（Peer-to-Peer）に通信を行います。

- **ソケット通信**: 各ノードは起動時にTCPポート `5000` 番で別スレッドとしてサーバー機能を常時稼働させ、他ノードからの接続を待ち受けます。
- **データ形式**: ブロックやチェーンの配列データは、すべてJSON形式にシリアライズされて送受信されます。
- **ブロードキャスト**: 新しいブロックをマイニングした場合や、チェーンの同期を要求する場合、ノードはリストアップされた全ピア（`10.0.0.1` ~ `10.0.0.3`）へ順にメッセージを送信します。

---

## 5. データ同期とコンセンサスアルゴリズム（Syncの仕組み）
分散ネットワークでは、「複数のノードが同時にマイニングに成功する」「通信遅延で一部のノードだけ古い状態になっている」といった理由で、チェーンの分岐（フォーク）や不整合が発生します。これを解決し、全ノードで一つの正しいデータ状態を合意（コンセンサス）するための仕組みが以下になります。

### 5.1 ブロック受信時の検証（Verify & Append）
他ノードから新しいブロック（`new_block`）を受信した場合、無条件に受け入れることはしません。
1. 受信したブロックの `prev_hash` が、自分が持っている最新ブロックの `hash` と一致しているか確認します。
2. ブロック自体のハッシュが正しく計算されているか、およびPoWの難易度条件を本当に満たしているかを再計算して検証します。
3. 検証に合格した場合のみ、自身のチェーンに追加します。もしチェーンが繋がっていなかった場合、自身のデータが古くなっていると判断し、後述のチェーン同期（`sync`）が必要となります。

### 5.2 最長チェーンの選択（Longest Chain Rule）による同期
ノードが手動、または不整合を検知した際に `sync` コマンドを実行すると、以下のプロセスでネットワーク全体のコンセンサスを図ります。

1. **同期要求の送信**:
   ノードは全ピアに対して `request_chain`（チェーン全体のデータを要求するメッセージ）を送信します。
2. **チェーンの受信**:
   要求を受け取った他ノードは、自身が保持しているチェーン全体を `chain_response` として送り返します。
3. **最長チェーンアルゴリズムの適用**:
   同期を要求したノードは、他ノードから受け取ったチェーンデータを一つずつ調べ、以下の条件で自身のチェーンを置き換えるか判断します。
   - **長さの比較**: ブロックチェーンの原則である「最も多くの計算量（PoW）が投じられたチェーンを正とする」というルールに従い、**受信したチェーンが自分のチェーンよりも長い場合のみ**採用を検討します。
   - **全体の妥当性検証**: 長いチェーンであっても、中身が改ざんされていては意味がありません。受信したチェーンのジェネシスブロックから最新のブロックまでを全て再計算し、「ハッシュの不一致はないか」「すべてのPoW条件が満たされているか」をチェックします。
   - **チェーンの置き換え（Replace）**: 全ての検証をパスした場合のみ、自身の古いチェーンを破棄し、新しい長いチェーンで上書きします。

この仕組みにより、一時的にネットワークが分岐したとしても、最終的には最も長く、かつ不正のないチェーンに全ノードのデータが収束（同期）します。

---

## 6. 発展実験：トラフィック制御（`tc`）を用いたネットワーク遅延のシミュレーション
情報ネットワーク特論のテーマ性をより深めるため、Linuxの `tc` (Traffic Control) コマンドと `netem` (Network Emulator) モジュールを利用し、実際のインターネット環境に近い「通信遅延」や「パケットロス」を仮想ネットワーク上に意図的に発生させる実証実験が可能です。

### 6.1 実験手順
任意のノード（例: `node2`）の仮想インターフェース（`veth2`）に対して、意図的な遅延（例: 200ms）とパケットロス（例: 5%）を注入します。

```bash
# Windows(PowerShell)から実行する場合
wsl sudo ip netns exec node2 tc qdisc add dev veth2 root netem delay 200ms loss 5%
```

### 6.2 観察できる現象（レポートの考察ポイント）
1. **ブロードキャストの遅延**: `node1` でブロックをマイニングした後、`node2` に伝搬して画面に表示されるまでに明らかなタイムラグ（200ms〜）が生じるようになります。
2. **パケットロスによる伝搬失敗**: 設定した確率（5%など）でブロードキャストのTCPパケットがロストし、タイムアウトによって `node2` が新しいブロックを取りこぼす事象が発生します。
3. **フォーク（チェーンの分岐）の頻発**: 通信が遅れて届かない間に、`node1` と `node2` がお互いを知らないまま独立してマイニングに成功してしまう確率が跳ね上がり、意図的に不整合を作り出せます。
4. **コンセンサスによる解決**: 分岐が発生した後、手動で `sync` を実行することで、悪環境下でも最終的に最長チェーンが選択され、データが正しく同期・収束する様子を観測できます。

### 6.3 環境の復元（元に戻す場合）
実験後は以下のコマンドで `tc` の設定を削除し、元の遅延のないクリーンな通信環境に戻すことができます。

```bash
# 設定を削除
wsl sudo ip netns exec node2 tc qdisc del dev veth2 root
```
